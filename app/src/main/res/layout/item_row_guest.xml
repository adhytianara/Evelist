<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="5dp">

    <ImageView
        android:id="@+id/img_guest"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/guest_image"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/tv_guest_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/img_guest"
        app:layout_constraintStart_toStartOf="@+id/img_guest"
        app:layout_constraintTop_toBottomOf="@+id/img_guest"
        tools:text="Guest name" />

    <TextView
        android:id="@+id/tv_guest_birthdate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        app:layout_constraintEnd_toEndOf="@+id/img_guest"
        app:layout_constraintStart_toStartOf="@+id/img_guest"
        app:layout_constraintTop_toBottomOf="@+id/tv_guest_name"
        tools:text="Birthdate" />

    <TextView
        android:id="@+id/tv_guest_month_is_prime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        app:layout_constraintEnd_toEndOf="@+id/img_guest"
        app:layout_constraintStart_toStartOf="@+id/img_guest"
        app:layout_constraintTop_toBottomOf="@+id/tv_guest_birthdate"
        android:text="@string/month_is_prime" />

</androidx.constraintlayout.widget.ConstraintLayout>